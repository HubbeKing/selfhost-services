apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: authelia
spec:
  clusterIP: None
  ports:
    - port: 6379
      name: redis
    - port: 26379
      name: sentinel
  selector:
    app: redis
