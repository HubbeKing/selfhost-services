apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: bazarr
spec:
  tls:
    - hosts:
      - bazarr.hubbe.club  
  rules:
    - host: bazarr.hubbe.club
      http:
        paths:
          - path: /
            backend:
              serviceName: bazarr
              servicePort: 6767
