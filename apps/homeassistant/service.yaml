apiVersion: v1
kind: Service
metadata:
  name: homeassistant
  annotations:
    traefik.ingress.kubernetes.io/service.passHostHeader: "false"
spec:
  externalName: homeassistant.lan
  ports:
    - name: http
      port: 8123
  type: ExternalName
