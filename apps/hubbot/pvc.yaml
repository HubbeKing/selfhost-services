apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-hubbot-dbchat
spec:
  storageClassName: iscsi-volume
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
