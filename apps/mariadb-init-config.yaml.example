apiVersion: v1
kind: ConfigMap
metadata:
  name: mariadb-init-config
stringData:
  create-dbs-and-users.sql: |
    CREATE DATABASE test;
    CREATE USER test IDENTIFIED BY 'test';
    GRANT ALL PRIVILEGES ON test.* TO 'test'@'%';
    FLUSH PRIVILEGES;
