apiVersion: v1
kind: Service
metadata:
  name: synapse-web
spec:
  ports:
    - port: 8008
  selector:
    app: synapse-web
---
apiVersion: v1
kind: Service
metadata:
  name: synapse-db
spec:
  selector:
    app: synapse-db
  ports:
    - port: 5432
