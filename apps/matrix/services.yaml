apiVersion: v1
kind: Service
metadata:
  name: matrix-synapse
spec:
  ports:
    - port: 8008
  selector:
    app: matrix-synapse
---
apiVersion: v1
kind: Service
metadata:
  name: matrix-db
spec:
  ports:
    - port: 5432
  selector:
    app: matrix-db
