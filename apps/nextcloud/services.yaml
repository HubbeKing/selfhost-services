apiVersion: v1
kind: Service
metadata:
  name: nextcloud-db
spec:
  ports:
    - port: 3306
  selector:
    app: nextcloud
    component: mariadb
---
apiVersion: v1
kind: Service
metadata:
  name: nextcloud-redis
spec:
  ports:
    - port: 6379
  selector:
    app: nextcloud
    component: redis
---
apiVersion: v1
kind: Service
metadata:
  name: nextcloud-collabora
spec:
  ports:
    - port: 9980
  selector:
    app: nextcloud
    component: collabora
---
apiVersion: v1
kind: Service
metadata:
  name: nextcloud-web
spec:
  ports:
    - port: 443
  selector:
    app: nextcloud
    component: nextcloud
