apiVersion: apps/v1
kind: Deployment
metadata:
  name: nextcloud-web
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nextcloud-web
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: nextcloud-web
    spec:
      containers:
        - image: nextcloud:18.0.2
          name: nextcloud
          resources:
            requests:
              cpu: "100m"
              memory: "500Mi"
          env:
            - name: MYSQL_HOST
              value: nextcloud-db
            - name: REDIS_HOST
              value: nextcloud-redis
            - name: NEXTCLOUD_DATA_DIR
              value: "/data"
          envFrom:
            - secretRef:
                name: nextcloud-envs
          volumeMounts:
            - name: nextcloud-config
              mountPath: /var/www/html
            - name: nextcloud-data
              mountPath: /data
            - name: nextcloud-array
              mountPath: /array
      volumes:
        - name: nextcloud-config
          persistentVolumeClaim:
            claimName: nextcloud-config-hostpath
        - name: nextcloud-data
          persistentVolumeClaim:
            claimName: nextcloud-data-hostpath
        - name: nextcloud-array
          persistentVolumeClaim:
            claimName: array
