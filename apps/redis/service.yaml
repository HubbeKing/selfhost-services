apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: kube-system
spec:
  loadBalancerIP: 192.168.1.6
  ports:
    - port: 6379
      name: redis
  selector:
    app: redis
  type: LoadBalancer
