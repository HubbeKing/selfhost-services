apiVersion: v1
kind: ConfigMap
metadata:
  name: weechat-configs
data:
  start.sh: |
    #!/bin/bash

    # start screen daemonized weechat
    screen -T screen-256color -U -D -m weechat
  attach.sh: |
    #!/bin/bash
    TERM=screen-256color screen -r "$@"
