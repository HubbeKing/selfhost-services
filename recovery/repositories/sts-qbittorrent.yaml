apiVersion: stash.appscode.com/v1alpha1
kind: Repository
metadata:
  name: sts-qbittorrent
  namespace: default
spec:
  backend:
    local:
      mountPath: /safe/data
      persistentVolumeClaim:
        claimName: nfs-backups
      subPath: qbittorrent
    storageSecretName: stash-restic-secret
