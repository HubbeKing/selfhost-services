apiVersion: stash.appscode.com/v1alpha1
kind: Repository
metadata:
  name: sts-resilio-sync
  namespace: default
spec:
  backend:
    local:
      mountPath: /safe/data
      persistentVolumeClaim:
        claimName: nfs-backups
      subPath: resilio-sync
    storageSecretName: stash-restic-secret
