apiVersion: stash.appscode.com/v1beta1
kind: RestoreSession
metadata:
  name: restore-bazarr
spec:
  repository: 
    name: sts-bazarr
  rules:
    - paths:
        - /config
  target:
    replicas: 1
    volumeMounts:
      - name: config-bazarr
        mountPath: /config
    volumeClaimTemplates:
      - metadata:
          name: config-bazarr-${POD_ORDINAL}
        spec:
          accessModes: [ "ReadWriteOnce" ]
          storageClassName: "longhorn"
          resources:
            requests:
              storage: 1Gi
