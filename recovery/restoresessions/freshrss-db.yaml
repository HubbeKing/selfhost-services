apiVersion: stash.appscode.com/v1beta1
kind: RestoreSession
metadata:
  name: restore-freshrss-db
spec:
  repository: 
    name: sts-freshrss-db
  rules:
    - paths:
        - /var/lib/mysql
  target:
    replicas: 1
    volumeMounts:
      - name: data-freshrss-db
        mountPath: /var/lib/mysql
    volumeClaimTemplates:
      - metadata:
          name: data-freshrss-db-${POD_ORDINAL}
        spec:
          accessModes: [ "ReadWriteOnce" ]
          storageClassName: "longhorn"
          resources:
            requests:
              storage: 1Gi
