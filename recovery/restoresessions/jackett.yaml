apiVersion: stash.appscode.com/v1beta1
kind: RestoreSession
metadata:
  name: restore-jackett
spec:
  repository: 
    name: sts-jackett
  rules:
    - paths:
        - /config
  target:
    replicas: 1
    volumeMounts:
      - name: config-jackett
        mountPath: /config
    volumeClaimTemplates:
      - metadata:
          name: config-jackett-${POD_ORDINAL}
        spec:
          accessModes: [ "ReadWriteOnce" ]
          storageClassName: "longhorn"
          resources:
            requests:
              storage: 1Gi
