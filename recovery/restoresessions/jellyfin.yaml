apiVersion: stash.appscode.com/v1beta1
kind: RestoreSession
metadata:
  name: restore-jellyfin
spec:
  repository: 
    name: sts-jellyfin
  rules:
    - paths:
        - /config
  target:
    replicas: 1
    volumeMounts:
      - name: config-jellyfin
        mountPath: /config
    volumeClaimTemplates:
      - metadata:
          name: config-jellyfin-${POD_ORDINAL}
        spec:
          accessModes: [ "ReadWriteOnce" ]
          storageClassName: "longhorn"
          resources:
            requests:
              storage: 5Gi
