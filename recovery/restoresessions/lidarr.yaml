apiVersion: stash.appscode.com/v1beta1
kind: RestoreSession
metadata:
  name: restore-lidarr
spec:
  repository: 
    name: sts-lidarr
  rules:
    - paths:
        - /config
  target:
    replicas: 1
    volumeMounts:
      - name: config-lidarr
        mountPath: /config
    volumeClaimTemplates:
      - metadata:
          name: config-lidarr-${POD_ORDINAL}
        spec:
          accessModes: [ "ReadWriteOnce" ]
          storageClassName: "longhorn"
          resources:
            requests:
              storage: 1Gi
