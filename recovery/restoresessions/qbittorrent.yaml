apiVersion: stash.appscode.com/v1beta1
kind: RestoreSession
metadata:
  name: restore-qbittorrent
spec:
  repository: 
    name: sts-qbittorrent
  rules:
    - paths:
        - /config
  target:
    replicas: 1
    volumeMounts:
      - name: config-qbittorrent
        mountPath: /config
    volumeClaimTemplates:
      - metadata:
          name: config-qbittorrent-${POD_ORDINAL}
        spec:
          accessModes: [ "ReadWriteOnce" ]
          storageClassName: "longhorn"
          resources:
            requests:
              storage: 1Gi
