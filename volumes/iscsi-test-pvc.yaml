kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: testclaim
  annotations:
    volume.beta.kubernetes.io/storage-class: "isci-volume"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
