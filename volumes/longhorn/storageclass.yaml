kind: StorageClass
apiVersion: storage.k8s.io/v1
metadata:
  name: longhorn
provisioner: driver.longhorn.io
allowVolumeExpansion: true
parameters:
  numberOfReplicas: "2"
  recurringJobs: '[{"name":"DailyBackup", "task":"backup", "cron":"00 00 * * *", "retain":7}]'
