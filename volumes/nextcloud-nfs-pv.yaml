apiVersion: v1
kind: PersistentVolume
metadata:
  name: nextcloud-data
  labels:
    bucket: shared
spec:
  capacity:
    storage: 100G
  volumeMode: Filesystem
  storageClassName: slow
  persistentVolumeReclaimPolicy: Retain
  accessModes:
    - ReadWriteMany
  nfs:
    server: "192.168.1.213"
    path: "/mnt/hubbe/array/Nextcloud"
    readOnly: false
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nextcloud-data
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: slow
  resources:
    requests:
      storage: 100G
  selector:
    matchLabels:
      bucket: shared
